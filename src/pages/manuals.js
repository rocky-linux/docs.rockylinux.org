import * as React from "react";
import Page from "../components/Page";

export default () => {
    return (
        <Page meta={{ title: 'Manuals' }}>
            <h1>Manuals</h1>
        </Page>
    );
};