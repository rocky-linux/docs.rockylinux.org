import * as React from 'react';
import Page from '../components/Page';

export default () => {
    return (
        <Page meta={{ title: 'Guides' }}>
            <h1>Guides</h1>
        </Page>
    );
};
